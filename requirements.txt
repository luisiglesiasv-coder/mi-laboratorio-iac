# requirements.txt

# 1. Herramienta principal de Infraestructura como Código (IaC)
ansible==9.1.0  
# Nota: Si usas Ansible Core, el paquete se llama 'ansible-core' a partir de la v2.11.

# 2. Frameworks de Pruebas y BDD
pytest==8.4.2
pytest-bdd==8.1.0
pytest-testinfra==10.1.1
behave==1.2.6  # <-- Requerido para el ejecutable 'behave'

# 3. Utilidades esenciales de Conexión, Testing y Vault
requests
urllib3<2.0        # Soluciona la advertencia NotOpenSSLWarning en macOS
PyYAML             # Para trabajar con inventarios y configuraciones YAML/JSON
psycopg2-binary    # Conexión real a PostgreSQL (más robusto)
redis              # Conexión a Redis
hvac               # Cliente Python para la API de HashiCorp Vault
molecule
molecule-plugins[docker]
molecule-plugins[testinfra]