# requirements.txt

# --- 1. Infrastructure as Code ---
ansible==9.1.0  # Ansible 9.1.0 includes ansible-core 2.16

# --- 2. Testing & BDD Frameworks ---
pytest==8.4.2
pytest-bdd==8.1.0
pytest-testinfra==10.1.1
# behave==1.2.6  # Only required if using the standalone 'behave' executable

# --- 3. Connection, Security & Database Utilities ---
requests
urllib3<2.0        # Fixes NotOpenSSLWarning/SSL compatibility on macOS runners
PyYAML             # Required for Ansible and Molecule configuration parsing
psycopg2-binary    # PostgreSQL adapter for integration testing (robust binary version)
hvac               # Official Python client for HashiCorp Vault API
redis              # Connection utility for Redis (if used in other scenarios)

# --- 4. Molecule & Orchestration ---
molecule>=6.0.0
molecule-plugins[docker]>=2.3.0
molecule-plugins[testinfra]>=0.0.1
# 🔴 EL CAMBIO CLAVE: Fijamos docker a una versión estable pre-7.0
docker==6.1.3