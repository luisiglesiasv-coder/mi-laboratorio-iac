name: BDD Test Workflow

on: [push]

jobs:
  bdd_tests:
    runs-on: self-hosted # O la etiqueta de tu Runner (ej. web-node)
    
    steps:
    
      - name: Checkout del Código
        uses: actions/checkout@v4

      # 🔑 NUEVO PASO CRÍTICO: Instalar el cliente Vault CLI
      - name: Instalar Vault CLI
        uses: hashicorp/setup-terraform@v3 # Esta acción sirve para Vault también
        with:
          cli_config_path: ${{ github.repository_path }}
          cli_version: 1.16.0 # Versión reciente y estable
          # Nota: Esta acción automáticamente añade el binario 'vault' al PATH
      
          # ----------------------------------------------------
      # 🔑 PASOS CRÍTICOS: Obtener Credenciales de Vault
      # ----------------------------------------------------
      - name: Configurar Variables de Vault y Obtener Credenciales Dinámicas
        id: vault_creds
        shell: bash
        env:
          VAULT_ADDR: http://10.0.0.31:8200
          # Usaremos el token root fijo para IaC. En producción, usa VAULT_TOKEN de AppRole.
          VAULT_TOKEN: hvs.TlA2xCHPWSebAjrI9NTnxI3G
        run: |
          echo "VAULT_ADDR=$VAULT_ADDR"

          # 1. Leer el secreto dinámico de la DB
          SECRET_DATA=$(vault read -format=json database/creds/runner_role)
          
          # 2. Parsear el JSON y exportar las variables
          DB_USER=$(echo "$SECRET_DATA" | jq -r '.data.username')
          DB_PASS=$(echo "$SECRET_DATA" | jq -r '.data.password')
          
          # 3. Exportar variables a GitHub Actions Runner
          # Esto hace que DB_USER y DB_PASS estén disponibles para pasos posteriores
          echo "DB_USER=$DB_USER" >> $GITHUB_ENV
          echo "DB_PASS=$DB_PASS" >> $GITHUB_ENV
          
          echo "✅ Credenciales dinámicas obtenidas."

      - name: Instalar Dependencias de Python
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt
          
      # ----------------------------------------------------
      # 🚀 EJECUTAR PRUEBAS
      # ----------------------------------------------------
      - name: Ejecutar Pruebas BDD Behave
        shell: bash
        # Las variables DB_USER y DB_PASS ahora están disponibles automáticamente
        run: |
          source venv/bin/activate
          # El código Python de la prueba debe leer las variables de entorno:
          # os.environ.get('DB_USER'), os.environ.get('DB_PASS'), os.environ.get('DB_HOST')
          export DB_HOST=10.0.0.31
          behave features/