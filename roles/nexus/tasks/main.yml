---
# roles/nexus/tasks/main.yml

# ==========================================
# SECTION 0: Prerequisites - Docker Setup
# ==========================================

- name: 0.1 Check if Docker is installed
  command: docker --version
  register: docker_check
  ignore_errors: yes
  changed_when: false

- name: 0.2 Install Docker and SDK (Ubuntu 24.04/25.10 Compatible)
  become: true
  when: docker_check.failed
  block:
    - name: Create directory for Docker GPG key
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'

    - name: Install system dependencies
      apt:
        name: [curl, ca-certificates, gnupg]
        state: present
        update_cache: yes

    - name: Add Docker GPG key (Correct binary format)
      shell: |
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor --yes -o /etc/apt/keyrings/docker.gpg
      args:
        creates: /etc/apt/keyrings/docker.gpg

    - name: Add Docker official repository
      apt_repository:
        repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu jammy stable"
        state: present
        filename: docker
        update_cache: yes

    - name: Install Docker Engine and Python SDK
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
          - python3-docker
          - python3-requests
        state: present

# ==========================================
# SECTION 1: Infrastructure and NFS Storage
# ==========================================

- name: 1.0 Create cosmetic "{{ nexus_nas_user }}" user for NFS mapping
  become: true
  user:
    name: "{{ nexus_nas_user }}"
    uid: "{{ nexus_nas_uid }}"
    group: users
    shell: /bin/false
    create_home: no
    state: present
  
- name: 1.1 Ensure nfs-common is installed
  become: true
  apt:
    name: nfs-common
    state: present

- name: 1.2 Create local mount point
  become: true
  file:
    path: "{{ nexus_mount_point }}"
    state: directory
    mode: '0777'

- name: 1.3 Mount NAS shared folder (Ethernet Stable Mode)
  become: true
  mount:
    path: "{{ nexus_mount_point }}"
    src: "{{ nexus_nas_ip }}:{{ nexus_nas_path }}"
    fstype: nfs
    # We use standard and robust options for Ethernet connection
    opts: rw,hard,noatime
    state: mounted

# ==========================================
# SECTION 2: Nexus Container Deployment
# ==========================================

# We always clean the previous container to apply new port/config changes
- name: 2.3 Force remove old container to apply new ports
  become: true
  shell: docker rm -f {{ nexus_container_name }}
  ignore_errors: yes

- name: 2.4 Run Sonatype Nexus 3 Container
  become: true
  shell: |
    docker run -d \
      --name {{ nexus_container_name }} \
      --restart always \
      --memory {{ nexus_memory_limit }} \
      -p 8081:8081 \
      -p 8082:8082 \
      -v {{ nexus_mount_point }}:/nexus-data \
      {{ nexus_image }} 

  # Note: Added -p 8082:8082 for the dedicated Docker connector port